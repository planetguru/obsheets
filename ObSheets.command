#!/bin/bash
# ObSheets — double-click this file to start the web app.
# Your browser opens automatically; keep this window open while you work.
cd "$(dirname "$0")"
python3 app.py
echo ""
echo "Press Enter to close..."
read
